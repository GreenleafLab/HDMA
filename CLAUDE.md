# HDMA-public

This repo ships an `hdma` agent skill (`.claude/skills/hdma/`) that routes you to
how to use the atlas **data** (on Zenodo) and **code**. In Claude Code it
triggers automatically when you ask about HDMA data or code; you can also invoke
it explicitly with `/hdma`.

@AGENTS.md
